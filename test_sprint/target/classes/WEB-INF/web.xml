<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <!-- Configuration du package à scanner adapté à ton arborescence -->
    <context-param>
        <param-name>packageToScan</param-name>
        <param-value>mg.itu.p4239.annotation.controller</param-value>
    </context-param>

    <!-- Configuration de ta DataSource PostgreSQL -->
    <context-param>
        <param-name>db_url</param-name>
        <param-value>jdbc:postgresql://localhost:5432/testdb</param-value>
    </context-param>
    <context-param>
        <param-name>db_user</param-name>
        <param-value>postgres</param-value>
    </context-param>
    <context-param>
        <param-name>db_password</param-name>
        <param-value>postgres</param-value>
    </context-param>

    <!-- Initialisation globale (Conteneur IoC + Connexion DB) -->
    <listener>
        <listener-class>com.passerelle.listener.FrameworkListener</listener-class>
    </listener>

    <!-- Servlet unique d'interception -->
    <servlet>
        <servlet-name>FrontController</servlet-name>
        <servlet-class>com.passerelle.controller.FrontControllerServlet</servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>FrontController</servlet-name>
        <url-pattern>/*</url-pattern>
    </servlet-mapping>

</web-app>